Листнете всички файлове в /var/log и сложете съдържанието във файл ~/file-list
Листнете всички файлове в /etc и ги ДОБАВЕТЕ към ~/file-list
Проверете дали съдържанието и на двете директории е вътре

Листене всички файлове в ~ и в ~/nosuchdir с една команда (тази директория/файл НЕ трябва да съществува). Направете така ,че да:
1. Да не виждате грешката (STDERR)
2. Да не виждате това от екрана (STDOUT)
3. Да не виждате и грешката и това от екрана (STDOUT + STDERR)



КАТО ОБИКНОВЕН ПОТРЕБИТЕЛ
Копирайте файла "shadow" в  ~/log-list направете така ,че да НЕ виждате грешката "permission denied" на екрана .(Ако не виждате грешки значи СТЕ като root ,а трябва да сте като обикновен потребител

Пуснете програмата "sleep 10000" в background режим
Пуснете втора програмата "sleep 20000" в background режим
Пуснете трета програмата "sleep 30000" в background режим

С помоща на "ps" намерете кой потребител е изпълнил програмите и какво е тяхното PID число

Върнете на foreground втората програма "sleep 3000" - направете така ,че процеса да спре след като вече сте го пуснали на foreground
проверете състоянието на процесите .... какво е то ?

Прекратете / убийте всички sleep процеси.

Допълнителна домашна:
Като root изпълнете комнадата ifconfig. Като използвате pipe | и команди като head , tail , grep , cut изкарайте на екрана сямо вашият ИП адрес който трябва да бъде 10.0.2.15
